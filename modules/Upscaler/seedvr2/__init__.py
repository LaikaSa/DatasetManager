"""SeedVR2 (numz/ComfyUI-SeedVR2_VideoUpscaler) vendored package.

Contains the full inference source tree (``src``), the 3B config
(``configs_3b``) and the text-embedding assets (``pos_emb.pt`` / ``neg_emb.pt``)
required by the upscaler. The heavy DiT/VAE weight files are downloaded on
demand into ``<project>/models/SeedVR2``.
"""
